# Median Browser 2.1.6 — Opt1 优化报告

本次修改采用“低风险、保持功能和数据格式不变”的原则，集中处理运行时阻塞、后台任务堆积和规则生成热路径。未修改 applicationId、版本号、备份结构、书签/历史数据库格式或 WebView 安全边界。

## 1. 浏览数据持久化

修改文件：`BrowserDataStore.java`

- JSON 序列化不再全程持有数据锁。现在只在锁内复制书签、历史和会话列表，再在后台线程锁外构建 JSON。
- 书签导出同样先取得轻量快照，避免大量书签导出时阻塞地址访问、历史记录或会话更新。
- 已规范化的 URL 不再在列表扫描中重复 `trim` 和去尾斜杠。
- 删除不存在的书签、清空空历史、导入零条新书签时不再触发无意义写盘。
- 原子写盘失败后会保留 dirty 状态，并以单一、受控的 5 秒重试恢复；新的真实数据修改会替换旧重试，不会无限叠加回调。
- `close()`、`flush()` 与延迟写入的状态切换统一放入同一锁，降低生命周期边界上的竞态风险。

直接收益：浏览库接近上限时，UI 线程等待数据锁的时间由“复制 + 数千项 JSON 构建”缩短为“仅复制列表”；瞬时 I/O 失败不再需要等下一次用户操作才有机会重新保存。

## 2. 过滤订阅更新队列

修改文件：`FilterSubscriptionStore.java`

- 增加单次更新门闩；重复触发不会继续进入固定线程池的无界等待队列。
- 订阅服务器很慢时，不再累积整轮网络任务、回调和 Activity 引用。
- executor 拒绝任务时恢复门闩状态，避免永久显示“更新中”。
- 服务关闭后禁止迟到回调重新引用已经销毁的界面。
- `readStream` 不再擅自关闭调用者传入的流，资源所有权改为由调用点明确管理。

直接收益：重复点击、生命周期重建或未来新增调用入口时，最多只有一轮订阅更新在执行，不会按点击次数线性增加网络、内存和等待时间。

## 3. 广告外观规则生成

修改文件：`AdBlockEngine.java`

- CSS 选择器与过程规则去重由 `ArrayList.contains` 改为 `HashSet`。
- 保留原有插入顺序、例外规则、每页数量上限和缓存行为。

在 900 条互不重复的页面选择器上，旧逻辑最多需要约 404,550 次线性相等比较；新逻辑为约 900 次集合插入/查询。真实收益取决于订阅规则重叠程度和设备性能，但复杂度已从近似 O(n²) 降为 O(n)。

## 4. 回归门禁

修改文件：`tools/static_checks.sh`

新增静态门禁，防止以下优化以后被意外回退：

- 浏览数据在锁外序列化；
- 写盘失败存在受控重试；
- 广告选择器使用集合去重，禁止重新出现 `target.contains(selector)`；
- 过滤订阅使用单次更新门闩。

## 验证结果

已通过：

- 53 个 Java 文件语法检查；
- Android XML 解析；
- NetworkSecurity、下载重试、地址栏分类、主页策略、自定义 CSS/HTML、主页配置和 Logo 回归测试；
- 用户脚本生成 JavaScript 语法测试；
- 项目安全静态门禁；
- 三个修改类在最小 Android API / JSON 桩环境下的 Java 17 编译。

当前执行环境无法连接 Gradle 分发站，也未预装 Android SDK，因此没有重新生成 APK、运行 Android Lint、R8 或真机性能测试。正式发布前仍需在 JDK 17 + Android SDK 36 环境执行 `./tools/verify.sh`，并完成真机冷启动、PSS、多标签和长时间订阅更新测试。
