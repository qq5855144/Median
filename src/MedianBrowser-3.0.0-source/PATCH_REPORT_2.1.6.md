# Median Browser 2.1.6 patch report

- Package/applicationId remains `com.xinyv.median.compat`.
- Version is now `69 / 2.1.6`.
- The custom-logo example is no longer persisted before Save.
- Canceling the custom-logo editor leaves the prior style and code untouched.
- A one-time migration removes only the exact 2.1.5 leaked `我的主页` example when it conflicts with the default `Median` title.
- The personalization sheet now uses explicit primary labels, secondary state/help text, and trailing chevrons.
- The main personalization row is now simply `页面布局`; its current mode is shown only after entering the section.
